on:
  release:
    types: [published]

jobs:
  github-releases-to-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Cpp (C++ / C)
        uses: aminya/setup-cpp@v0.43.0

      - name: Build c++ project
        uses: nicledomaS/cmake_build_action@v1.4
